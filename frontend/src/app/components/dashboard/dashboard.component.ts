import { Component } from '@angular/core';

@Component({
  selector: 'app-dashboard',
  standalone: true,
  template: `<div>Dashboard Component Skeleton</div>`
})
export class DashboardComponent {
  // Se implementará en detalle en los siguientes pasos.
}

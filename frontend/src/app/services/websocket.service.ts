import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class WebsocketService {
  // El servicio de WebSocket se implementará en detalle en los siguientes pasos.
  constructor() {}
}
